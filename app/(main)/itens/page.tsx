import ClientPage from "./page.client";

export const metadata = {
    title: "Itens",
    description: "Lista de itens do grupo selecionado",
};

export default function Page() {
    return <ClientPage />;
}
